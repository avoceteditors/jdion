(ns dion.op
  (:gen-class))

;; Find Directory
(defn find-dir [path]
  
  (println "RUNNING PATH\n")
  (println path)
  )


;; Parse Directory
(defn parse-dir [path]
  (println "RUNNING PARSE\n")
  

  (path)
  )


